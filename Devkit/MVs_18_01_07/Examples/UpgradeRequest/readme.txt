硬件环境要求:

1. AP80系列开发板。
2. 外接串口小板或者直接使用AP80系列开发板上的串口，TX/RX/GND 接至 B6/B7/GND 引脚(波特率：115200,数据位：8,停止位：1,奇偶校验：无)。

操作方法：

1、该工程必须放置在SDK的Examples目录下使用，否则会编译不通过。
2、编译后，通过SW直接下载到Flash BankA中运行。程序一直等待PC机发送升级请求命令（U或u）。
3、收到PC串口发送的升级命令请求后，程序负责将升级信息通知到Bootloader。
